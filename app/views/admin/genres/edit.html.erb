<div class="container">
   <h4>ジャンル編集</h4>
     <div class="container">
       <%= form_with model: @genre, url: admin_genre_path,method: :patch do |f| %>
       <%= f.label :name, "ジャンル名" %>
       <%= f.text_field :name %>
       <%= f.submit '変更を保存' %>
      <% end %>
     </div>
</div>