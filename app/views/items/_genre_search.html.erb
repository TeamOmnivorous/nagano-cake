<table class="genres_search table table-bordered">
  <thead>
    <tr>
      <th><h3>ジャンル検索</h3></th>
    </tr>
  </thead>
  <tbody>
      <% genres.each do |genre| %>
        <tr>
          <td>
            <%= link_to items_path(genre_id: genre.id), class: "text-dark" do %>
              <%= genre.name %>
            <% end %>
          </td>
        </tr>
      <% end %>
  </tbody>
</table>

